const IS_DEV = import.meta.env.DEV;

export const FORCE_ONBOARDING_TEST = IS_DEV && false;
export const FORCE_PROFILE_INTRO_TEST = IS_DEV && false;
export const FORCE_PROFILE_INTRO_LOOP = IS_DEV && false;
